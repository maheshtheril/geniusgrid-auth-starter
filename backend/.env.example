APP_PUBLIC_URL=http://localhost:5173
APP_URL=http://localhost:3000
SESSION_TTL_HOURS=12

# CORS
FRONTEND_ORIGIN=http://localhost:5173

# Database
PGHOST=localhost
PGPORT=5432
PGDATABASE=geniusgrid
PGUSER=saasdbuser
PGPASSWORD=changeme

# SMTP (dev: use MailHog or Papercut)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
MAIL_FROM=no-reply@geniusgrid.app
